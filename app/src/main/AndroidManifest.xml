<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- NH-11 audit verdict: KEEP. BlockerActivity is a normal activity, but on API 29+ the
         usage-stats poller (BlockingForegroundService.tryBlock) may only start it from the
         background while this grant is held — it's one of the documented background-activity-start
         exemptions. The accessibility path is exempt on its own (system-bound service), so this
         permission is what keeps app blocking alive when the user declines accessibility. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- NH-09: read the active network's LinkProperties so forwarded DNS goes to the user's own
         configured resolvers, not a hardcoded public one. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- SCHEDULE_EXACT_ALARM only (user grants "Alarms & reminders" on 14+; AlarmScheduler falls
         back to inexact when ungranted). USE_EXACT_ALARM is Play-restricted to alarm-clock/calendar
         core functionality, which a focus blocker is not — shipping it blocks Play review. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- QR save-to-gallery on API 26–28 only; API 29+ uses scoped MediaStore. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-feature android:name="android.hardware.nfc" android:required="false" />

    <!-- App picker enumerates launcher apps via queryIntentActivities(MAIN/LAUNCHER);
         the blocker also resolves the current HOME app to exempt it. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
        <!-- Dynamic browser detection: enumerate every app that handles web links so URL blocking
             covers unlisted browsers, not just a hardcoded set. Scoped web intent, not QUERY_ALL. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>
    </queries>

    <application
        android:name=".AscendyApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ascendy">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Ascendy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/vnd.ascendy.tag" />
            </intent-filter>

            <!-- Tech dispatch is scoped by @xml/nfc_tech_filter to NDEF-capable tags only. The bare
                 TAG_DISCOVERED filter that used to sit below is deliberately gone: it is the
                 lowest-priority catch-all, so Ascendy launched for every card the phone ever saw
                 (transit, hotel keys, bank cards). Pairing is unaffected — MainActivity still
                 registers TAG_DISCOVERED via foreground dispatch while it is on screen. -->
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />
        </activity>

        <activity
            android:name=".blocking.BlockerActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:noHistory="true"
            android:showOnLockScreen="true"
            android:theme="@style/Theme.Ascendy.Blocker" />

        <service
            android:name=".service.BlockingAccessibilityService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".service.BlockingForegroundService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="App blocking enforcement during user-initiated focus sessions" />
        </service>

        <service
            android:name=".vpn.AscendyVpnService"
            android:exported="false"
            android:stopWithTask="false"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="DNS sinkhole for website blocking during user-initiated focus sessions" />
        </service>

        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <!-- BOOT_COMPLETED only: the session/schedule state lives in the credential-encrypted
                 Room DB, which is unreadable before first unlock, so LOCKED_BOOT_COMPLETED (direct
                 boot) bought nothing and risked throwing against an unavailable DB. Restore happens
                 right after unlock; the safety timer re-derives correctly either way. -->
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".service.ScheduleAlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".widget.AscendyWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.ascendy.app.WIDGET_REFRESH" />
                <action android:name="com.ascendy.app.SESSION_STARTED" />
                <action android:name="com.ascendy.app.SESSION_ENDED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <receiver
            android:name=".service.SessionControlReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.ascendy.app.action.SESSION_TOGGLE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".service.AscendyDeviceAdminReceiver"
            android:exported="true"
            android:label="@string/device_admin_label"
            android:description="@string/device_admin_description"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".service.AscendyTileService"
            android:exported="true"
            android:icon="@drawable/ic_logo"
            android:label="Ascendy"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="false" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <activity
            android:name=".qr.PortraitCaptureActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme"
            android:windowSoftInputMode="stateAlwaysHidden"
            tools:ignore="LockedOrientationActivity" />
    </application>
</manifest>
