SelectableChip — the app-wide selectable pill (durations, days, goal minutes, theme options). Cloud fill + Mist hairline idle; Petal fill + on-Petal text selected; 14px padding, theme-medium radius, press-scales.

```jsx
<SelectableChip label="25 min" selected={dur === 25} onClick={() => setDur(25)} />
```
