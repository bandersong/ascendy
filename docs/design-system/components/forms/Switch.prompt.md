Switch — Material-3-style toggle (lockdown, strict mode, schedule enabled). Petal track + Cream thumb on; Cloud track + Mist hairline + Smoke thumb off.

```jsx
<Switch checked={lockdown} onChange={setLockdown} />
```
